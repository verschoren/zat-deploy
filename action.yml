# action.yml
name: 'ZAT Tools'
description: 'Update an existing app in a Zendesk instance. Requires a .zat file/'
branding:
  icon: 'arrow-up-circle'  
  color: 'green'
runs:
  using: 'docker'
  image: 'Dockerfile'
